<% if @feed_item.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_item %>
  </ol>
  <%= will_paginate @feed_item, renderer: BootstrapPagination::Rails %>
<% end %>